# Contract artifacts

Copy the compiled wasm here from anielab-contracts before running the deploy endpoint:

cp ../anielab-contracts/target/wasm32-unknown-unknown/release/revenue_splitter.wasm ./contracts/

WASM_PATH env var points here by default.

